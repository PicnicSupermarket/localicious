{

  {{#each PLURAL}}
  {{# value}}
  "{{ ../key }}.{{ quantity }}": "{{{ value }}}",
  {{/ value}}
  {{/each}}
  {{#each SINGULAR}}
  "{{ key }}": "{{{ value }}}"{{#unless @last}},{{/unless}}
  {{/each}}
}
